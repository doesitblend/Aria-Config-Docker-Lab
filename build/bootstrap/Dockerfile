FROM python:3.10
# This container is intended to be used as an executable to prepping 
# the project directories in order to build the remaining labs

RUN apt-get update && apt-get upgrade -y
COPY 
ENTRYPOINT ["/usr/bin
